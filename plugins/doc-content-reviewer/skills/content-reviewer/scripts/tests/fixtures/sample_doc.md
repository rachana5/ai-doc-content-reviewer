# Retry middleware

The default value for `maxRetries` is 3.

See the [rate limiting guide](./rate-limiting.md) for related configuration,
or the [upstream docs](https://example.com/retry) for background.
